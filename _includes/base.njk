<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <link rel="stylesheet" href="{{ '/styles/style.css' | url }}" />
    <link rel="shortcut icon" href="{{ '/images/StarFav.png' | url }}" type="image/x-icon">
    <title>Morgan's Journal</title>
  </head>
  <body>
    <div class="main-for-home">
      <div class="pop-up" id="pop-up">
        <p>Please turn your screen to landscape for better experience</p>
        <br />
        <button onclick="landscapePop()">Okay</button>
      </div>
      <div class="h-p-t">
        <div class="tabs">
          <button class="tab{% if tab == 'Intro' %}-open{% endif %}" onclick="location.href='{{'/' | url }}'">Intro</button>
          <button class="tab{% if tab == 'Stuff' %}-open{% endif %}" onclick="location.href='{{'/Journals/stuff/' | url }}'">Stuff</button>
          <button class="tab{% if tab == 'Personal' %}-open{% endif %}" onclick="location.href='{{'/Journals/personal/' | url }}'">Personal</button>
          <button class="tab{% if tab == 'Letters' %}-open{% endif %}" onclick="location.href='{{'/Journals/letters/' | url }}'">Letters</button>
          
        </div>
        <div class="home-panel">
            {{ content | safe}}
        </div>
      </div>
    </div>
    <script src="{{ '/script.js' | url }}"></script>
  </body>
</html>